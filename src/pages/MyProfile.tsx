import DayConfig from '../components/DayConfig';

export default function MyProfile() {
  return (
    <div className="page">
      <h1 className="page-title">我的训练计划</h1>
      <DayConfig />
    </div>
  );
}
